{
  "name": "asset-atlas-desktop",
  "version": "0.1.0",
  "private": true,
  "description": "Desktop Asset Atlas prototype with native folder scanning and copying.",
  "main": "src/main/main.cjs",
  "scripts": {
    "start": "electron .",
    "check": "node --check src/main/main.cjs && node --check src/main/preload.cjs && node --check src/renderer/app.js && node --check src/renderer/model-viewer.mjs",
    "build:windows": "electron-builder --win",
    "build:mac": "electron-builder --mac"
  },
  "dependencies": {
    "electron": "^31.7.7",
    "fflate": "^0.8.2",
    "three": "^0.166.1"
  },
  "devDependencies": {
    "electron-builder": "^24.13.3"
  },
  "build": {
    "appId": "local.asset-atlas.desktop",
    "productName": "Asset Atlas",
    "files": [
      "src/**/*",
      "package.json"
    ],
    "directories": {
      "output": "dist"
    }
  }
}
